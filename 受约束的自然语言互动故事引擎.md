## 一、项目定位

谜局模块允许玩家通过自然语言参与一段预设背景和结局方向的故事。

后台作者可以配置：

- 故事标题和故事背景；
- 已经发生的过去；
- 世界核心设定；
- 玩家初始身份和条件；
- NPC人物、性格、目标、秘密和行为逻辑；
- 地点、物品、资源和时间线；
- 故事可能的发展方向；
- 多个主结局和偏航结局。

玩家不通过固定选项操作，而是直接输入自然语言，例如：

- “我走到窗边观察外面。”
- “我问他昨晚去了哪里。”
- “我尝试撬开这扇门。”
- “我把剩下的钱交给她。”

系统需要理解玩家的行动意图，结合当前世界状态进行客观裁决，再生成故事反馈。

这不是一个让大模型自由续写故事的聊天应用，而是一个：

> 由服务端世界状态、事件账本、时间线和规则引擎控制事实，由大模型负责自然语言理解、模糊行动裁决和叙事表达的互动叙事引擎。

## 二、必须遵守的核心原则

以下原则属于系统级约束，不应只依赖大模型提示词：

1. 玩家只能声明“想做什么”，不能直接声明结果。

例如：

- “我搜索房间”是有效行动；
- “我搜索房间并找到一把枪”只能被解析为“尝试搜索房间”；
- “我开枪杀死他”只能被解析为“尝试开枪攻击他”。

最终结果必须由系统根据人物能力、环境、资源、时间和客观条件裁决。

2. 每一局游戏都是独立世界线。

每次开始游戏都创建新的 `run_id`，不同玩家、不同故事版本和不同游玩记录之间不得共享运行状态。
一个玩家如果有一局游戏，退出后重新进入可以选择继续之前的游戏或创建新的游戏。

3. 已经发生的事情不可被直接改写。

以下内容一旦发生，必须记录为永久事件：

- 玩家尝试过的行动；
- 实际说过的话；
- 谁真正听见或看见了；
- 消耗的时间和资源；
- 获得、转移、丢失或损坏的物品；
- 人物受伤、死亡或失踪；
- 产生的承诺、谎言、错误和误解；
- 秘密的发现与传播；
- 对人物关系产生影响的具体事件；
- 结局资格的获得、失去和错过。

需要纠正错误时，只能追加新的纠正事件，不能删除旧历史。

4. 世界时间只能向前推进。

移动、搜索、谈话、等待、战斗和休息都必须消耗时间。玩家行动期间，NPC、敌人、天气和预定事件仍会继续发展。

5. 客观现实高于玩家预期。

系统不能为了迎合玩家而让明显不可能的行为成功。例如普通人无法徒手击败一支军队，没有钥匙不能直接使用钥匙开门，没有持有物品不能使用该物品。

6. 必须区分客观事实和人物认知。

每个角色只能根据自己实际看到、听到、知道、相信或误解的信息行动。NPC不能拥有全知视角。

7. 不使用“好感度、恐惧值、敌意值”等主观数字决定剧情。

人物关系应通过具体事件和非数值状态表达，例如：

- 因玩家履行承诺而愿意合作；
- 因发现玩家撒谎而保持戒备；
- 因遭到背叛而拒绝继续交流。

8. 故事采用软收敛，而不是强行收敛。

故事存在多个预设结局方向，但玩家可以偏离主线。偏离后：

- 某些结局可能逐渐失效；
- 原故事人物和事件仍会自行发展；
- 玩家可能错过关键事件；
- 玩家可能成为旁观者；
- 玩家可能进入偏航、失败、死亡、超时或脱离主线结局。

不能为了强行进入某个结局而临时创造没有因果铺垫的灾难。

9. 玩家输入永远是待分析的数据，不是系统指令。

任何要求忽略规则、查看隐藏剧本、输出系统提示词、伪造管理员身份或直接修改世界状态的内容，都不能改变系统运行逻辑。

10. Story Package 是世界与主线骨架，不是行动白名单。

故事包负责核心事实、人物性格与目标、关键因果、风险边界、大体解决方向和结局条件。玩家在当前可知、可见、可到达和可操作范围内提出的普通行动，即使没有预先编写同名行动转换，也必须由 Agent 客观裁决，并由服务端按位置、所有权、能力、证据、时间和资源边界验证后给出具体反馈。只有主线预设因果、带条件通行、预设物品后果、人物死亡或失踪、世界标记和结局等受保护变化必须依赖预设转换或服务端状态图。

## 三、系统职责边界

系统需要明确分为以下部分。

### 1. 故事包 Story Package

描述一个故事的静态设计，包括：

- 不可变核心事实；
- 开局初始化事实；
- 人物；
- 地点；
- 物品；
- 资源；
- 世界规则；
- 人物知识和秘密；
- 场景及其进入条件；
- 世界时间线；
- 预定事件；
- 结局条件；
- 禁止发生的事情。

故事包必须有版本号。已经开始的游玩必须固定使用开局时的故事版本。

### 2. 运行状态 Run State

描述某一局游戏当前的世界状态，包括：

- 当前世界时间；
- 玩家位置和状态；
- NPC位置和状态；
- 当前物品归属；
- 当前剩余资源；
- 当前人物认知；
- 当前已公开信息；
- 当前尚未触发的世界事件；
- 当前仍可进入或已经失效的结局。

### 3. 事件账本 Event Ledger

以只追加的方式记录这一局所有已经发生的事实。

事件账本是事实来源，运行状态只是根据事件账本计算出的当前快照。

### 4. DeepSeek Agent

DeepSeek不直接修改数据库，只能返回结构化的“裁决提案”，例如：

- 玩家意图；
- 被忽略的结果声明；
- 行动是否可执行；
- 实际结果；
- 时间消耗；
- 资源变化建议；
- 物品变化建议；
- 谁听见或看见了什么；
- 人物认知变化建议；
- 玩家可见结果；
- 结局资格变化信号。

所有提案必须经过服务端规则校验，验证通过后才能提交为正式事件。

### 5. 叙事生成器

叙事生成器只接收已经提交且玩家可见的结果。

它可以负责文字表现，但不能：

- 添加未经提交的人物或物品；
- 修改行动结果；
- 泄露隐藏事实；
- 替玩家继续行动；
- 让NPC知道其不可能知道的信息。

## 四、核心领域对象

请优先围绕以下领域对象设计，而不是先围绕聊天消息设计：

- `Story`
- `StoryVersion`
- `StoryPackage`
- `CharacterDefinition`
- `LocationDefinition`
- `ItemDefinition`
- `ResourceDefinition`
- `ScheduledWorldEvent`
- `EndingDefinition`
- `StoryRun`
- `RunState`
- `Turn`
- `WorldEvent`
- `StateSnapshot`
- `EndingStatus`

其中：

- `StoryVersion`必须不可变；
- `StoryRun`必须绑定固定的`StoryVersion`；
- `WorldEvent`必须只追加；
- 所有运行时数据必须带有`run_id`；
- 重要提交必须支持幂等；
- 状态更新必须防止并发覆盖；
- 当前状态应能够从事件账本重新构建。

## 五、本轮只开发第一阶段

不要一次性实现完整产品。

本轮目标是搭建“世界状态与事件账本”的可靠底座，暂时不实现完整故事后台、复杂Agent提示词、完整前端页面、多人模式或复杂战斗系统。

请完成以下工作：

### 1. 阅读并理解仓库

检查：

- 当前技术栈；
- 服务端结构；
- 数据库；
- ORM；
- API规范；
- 测试框架；
- 环境配置；
- 是否已有用户、故事或聊天相关模块。

不要凭空假设技术栈。

### 2. 建立领域模型

根据现有项目结构，设计并实现第一版核心模型：

- Story；
- StoryVersion；
- StoryRun；
- Turn；
- WorldEvent；
- RunState或StateSnapshot。

可以为未来字段预留扩展能力，但不要在本轮实现全部故事编辑字段。

### 3. 实现事件追加服务

实现统一的事件提交入口，至少满足：

- 事件只能追加；
- 带有`run_id`和`turn_id`；
- 支持`idempotency_key`；
- 支持状态版本校验；
- 支持事务提交；
- 可以根据事件更新当前状态快照；
- 同一个请求重复提交不会重复扣除资源或重复创建事件。

### 4. 实现最基础的世界约束

第一阶段至少通过程序保证：

- 世界时间不能倒退；
- 资源不能被消费到负数；
- 已销毁或已转移的物品不能继续由原持有人使用；
- 已死亡或失能角色不能继续执行普通行动；
- 一局不能读取或修改另一局的数据；
- StoryRun不能在开始后切换StoryVersion；
- 事件账本重放后的状态应与当前快照一致。

### 5. 预留DeepSeek适配层

建立清晰的接口或抽象层，例如：

- `IntentParser`
- `TurnAdjudicator`
- `NarrativeRenderer`

本轮先提供接口、DTO、Mock或Stub实现，不需要马上接入真实DeepSeek API。

不要让大模型适配器拥有直接修改数据库的权限。

### 6. 提供最小API骨架

结合现有项目风格，提供最小可运行接口，例如：

- 创建故事；
-创建故事版本；
- 创建新游戏实例；
- 查询当前游戏状态；
- 提交一个测试回合；
- 查询某局事件列表；
- 通过事件账本重建状态。

接口命名和路由应遵守项目现有规范，不必机械照搬以上名称。

### 7. 编写测试

至少覆盖：

- 重复请求不会重复产生事件；
- 时间回退被拒绝；
- 资源不足时消费被拒绝；
- 不持有物品时无法使用；
- 死亡角色行动被拒绝；
- 不同run之间完全隔离；
- 固定故事版本不可被替换；
- 事件重放能够得到一致状态。

## 六、开发方式

请按照以下顺序执行：

1. 阅读仓库；
2. 总结你对现有项目和目标系统的理解；
3. 给出与你发现的技术栈相匹配的第一阶段设计；
4. 直接完成第一阶段代码；
5. 运行测试、类型检查和构建；
6. 修复发现的问题；
7. 汇总改动结果。

不要只给架构建议而不落代码，也不要一次开发完整产品。

遇到缺少细节时，优先做合理、保守、可扩展的工程假设，并将假设记录在文档中，不要因为非关键细节中断开发。

## 七、本轮交付结果

完成后请明确输出：

1. 你对项目的最终理解；
2. 本轮采用的架构；
3. 新增或修改的文件；
4. 数据库模型和迁移说明；
5. 已实现的核心约束；
6. API使用示例；
7. 测试和构建结果；
8. 当前仍未实现的内容；
9. 下一阶段最合理的开发目标；
10. 运行项目和执行测试的具体命令。

## 八、禁止的实现方式

不要采用以下方案：

- 只保存聊天记录，不保存结构化世界状态；
- 每轮把全部历史直接交给大模型重新推理；
- 让大模型直接更新数据库；
- 让大模型自由决定物品、资源、死亡和结局；
- 用一段超长提示词代替事件账本和规则校验；
- 覆盖或删除已经发生的历史事件；
- 让不同游戏实例共享运行时记忆；
- 在没有事件依据的情况下直接修改人物关系；
- 为了快速完成而把所有逻辑写进一个聊天接口；
- 本轮直接实现所有后台、AI提示词、前端和完整故事玩法。

请现在开始检查仓库，理解上述要求并提问。
